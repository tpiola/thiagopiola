import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Diagnóstico de Conversão Digital Grátis | Thiago Piola",
  description: "Descubra onde seu site está perdendo clientes. Diagnóstico gratuito em 3 minutos para negócios locais.",
};

export default function DiagnosticoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
